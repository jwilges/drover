packages:
  basic-lambda:
    region_name: us-east-1
    function:
      name: basic-lambda
      compatible_runtime: python3.8
      includes:
        - '^basic_lambda.*'
      extra_paths:
        - instance
    upload_bucket:
      region_name: us-east-1
      bucket_name: drover-examples
    publish: false